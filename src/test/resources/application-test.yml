#Profile: dev
restclient:
  url:
    jsonplaceholder-client: https://jsonplaceholder.typicode.com
    reqres-client: https://reqres.in/api

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  flyway:
    enabled: true